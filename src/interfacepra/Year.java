package src.interfacepra;
/*To access the interface methods, the interface must be "implemented"
(kinda like inherited) by another class with the implements
keyword (instead of extends). The body of the interface method is
provided by the "implement" class: */
public interface Year {
    // Interface
        public void mainSubjects(); // interface method (does not have a body)
        public void subSubject(); // interface method (does not have a body)
    }

